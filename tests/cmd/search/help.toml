bin.name = "rdoc"
args = ["search", "--help"]
status.code = 0
stdout="""
rdoc-search 
Returns a list of commits containing expression.

USAGE:
    rdoc search [OPTIONS] <terms>

ARGS:
    <terms>
            

OPTIONS:
    -a, --all
            Search in commit message and description

    -h, --help
            Print help information

    -t, --tag
            Search by tag

    -w, --author
            Search by author
"""
stderr = ""